apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admiral-server.fullname" . }}-test-connection"
  labels:
    {{- include "admiral-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admiral-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
